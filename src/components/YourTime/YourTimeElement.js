import styled from 'styled-components';

const YourTimeElement = styled.span`
  font-family: 'Inter', Arial, sans-serif;
  font-weight: 500;
  font-size: 16px;

  @media screen and (min-width: 768px) {
    font-size: 20px;
  }
`;

export default YourTimeElement;
